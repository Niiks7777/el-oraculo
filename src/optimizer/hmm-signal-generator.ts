/**
 * 🔮 El Oraculo Pro Feature
 *
 * This module is part of El Oraculo Pro.
 * Get it at: https://niiks7777.gumroad.com/l/el-oraculo-pro
 *
 * Pro includes:
 * - HMM Markov regime detection (+9% win rate)
 * - Nemotron LLM market predictions (free API)
 * - Karpathy autoresearch optimizer (20 iterations/cycle)
 * - Faithful backtest engine (ADX guard, risk guards)
 * - Self-evolving revenue goals
 * - Revenue attribution & skill evolution
 */

export function notAvailable(feature: string): void {
  console.log(`[El Oraculo] "${feature}" requires Pro. Get it at https://niiks7777.gumroad.com/l/el-oraculo-pro`)
}

// Stub exports to prevent build errors
export const hmm_signal_generator_stub = true

